export enum GameStatus {
    ON = "ON",
    WAITING = "WAITING",
    COMPLETE = "COMPLETE"
}

export enum UserStatus {
    WAITING = "WAITING",
    LOST = "LOST",
    WON = "WON",
    LEFT = "LEFT"
}
