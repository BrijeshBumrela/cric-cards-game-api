export enum GameStatus {
    ON = "ON",
    WAITING = "WAITING",
    COMPLETE = "COMPLETE"
}

export enum UserStatus {
    PLAYING = "PLAYING",
    WAITING = "WAITING",
    LOST = "LOST",
    WON = "WON",
    LEFT = "LEFT"
}
